[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "alembic upgrade head && exec uvicorn main:app --host 0.0.0.0 --port 8000"
healthcheckPath = "/api/auth/me"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
